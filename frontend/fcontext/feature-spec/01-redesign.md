

Read `AGENTS.md` before starting.


edit the globals.css file according to the attached file `the design-report` u just gave.
this is so that the u.i and colour scheme remains constitnet throughout and we dont hardcode raw colours againa and again  

We're adding the design system and UI primitive components.


Do not modify the generated `components/ui/*` files after installation dowload shadcn radix u.i base u.i new shadcn 2026 

ensure all components match the design theme created in `globals.css`.

clear the boiler plate code of next js remove all the unnesssary svg in public folder only keep favicon

and in layout.tsx add meta data of our project and other thing whcih will help in seo and geo 
make the lib folder according to the folder sturucture mentioned and setup providers if any required 
further setup constant folder and env.ts

Check when done: sussess criteria
- All components import without errors
- `cn()` works properly
- No default styling our own styling should be there
- boiler plate code removed 
- check tailwind and shadcn both working perfeclty


